language: python
python:
  - "3.6"

notifications:
  email: false

install:
    - pip install -q cython numpy networkx scipy scikit-learn pandas gensim joblib gensim psutil
    - pip install .

script:
  - pytest test/test.py